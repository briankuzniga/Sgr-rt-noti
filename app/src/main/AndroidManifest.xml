<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/trc_logo_off"
        android:label="@string/app_name"
        android:roundIcon="@drawable/trc_logo_off"
        android:supportsRtl="true"
        android:theme="@style/Theme.SgrApp">

        <activity android:name=".HelpActivity" android:exported="false" />
        <activity android:name=".NotificationsActivity" android:exported="false" />
        <activity android:name=".FindTrainActivity" android:exported="false" />

        <!-- 🚀 FIXED: Added TrackingActivity -->
<!--        <activity android:name=".TrackingActivity" android:exported="false" />-->

        <!-- Splash Screen (First Screen) -->
        <activity android:name=".SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Screens -->
        <activity android:name=".GetStartedActivity" android:exported="false" />
        <activity android:name=".ActivityLogin" android:exported="false" />
        <activity android:name=".ActivitySignUp" android:exported="false" />
        <activity android:name=".ActivityHome" android:exported="false" />

    </application>

</manifest>
